# Core compute — PyTorch backend (Phase 4+)
torch>=2.0.0
numpy>=1.26.0

# Environment / simulation
scipy>=1.11.0
scikit-learn>=1.3.0

# Config & I/O
pyyaml>=6.0.1

# Optional: visualization (not needed for --headless)
# pygame>=2.5.0
# matplotlib>=3.8.0

# Legacy JAX dependencies — not used in Phase 4/5
# jax>=0.4.23
# jaxlib>=0.4.23
# flax>=0.7.5
# optax>=0.1.7
